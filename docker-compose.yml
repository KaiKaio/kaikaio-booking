services:
  kaikaio-booking-h5:
    image: kaikaioano/kaikaio-booking:master
    container_name: kaikaio-booking
    ports:
      - "3001:80"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: unless-stopped
    mem_limit: 512m
